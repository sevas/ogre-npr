CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(ogre-npr)

IF(APPLE)
	SET(LIBRARY_OUTPUT_PATH build.osx)
	SET(EXECUTABLE_OUTPUT_PATH build.osx)
	SET(CONFIG_FILES_PATH config/osx/)
	INCLUDE_DIRECTORIES(/opt/local/include)
ENDIF()

ADD_SUBDIRECTORY(ogre-npr)